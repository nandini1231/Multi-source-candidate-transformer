# TC04 — Normalization: Skill Canonicalization

## Purpose
SKILL_SYNONYMS reverse lookup + fuzzy matching collapses many variants.

## Modules Validated
- normalize_skill (exact + fuzzy)
- FieldResolver.resolve_skills dedupe by canonical name

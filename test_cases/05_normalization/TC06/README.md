# TC06 — Normalization: Name Whitespace

## Purpose
Extra spaces around or inside names must not produce duplicate candidates or break matching.

## Modules Validated
- helpers.normalize_name_key
- CSV adapter cell.strip()

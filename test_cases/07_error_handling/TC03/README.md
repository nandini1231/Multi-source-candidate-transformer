# TC03 — Error Handling: CSV Headers Only

## Purpose
Header-only CSV is structurally valid but produces zero candidates.

## Modules Validated
- DictReader iteration over zero rows
- Pipeline empty result path

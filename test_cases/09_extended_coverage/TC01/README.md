# TC01 — years_experience from CSV column

Validates that the `years_experience` / `experience_years` CSV column is ingested
and projected via the default projection config.

**Modules:** `RecruiterCSVAdapter`, merge engine, `Projector`

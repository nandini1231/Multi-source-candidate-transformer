# TC02 — Projection: Field Rename via `from`

## Purpose
A field's output key can differ from its source path using the `from` attribute.

## Modules Validated
- ProjectionField.from_
- Projector._resolve_path mapping from source attribute
